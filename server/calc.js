export const sum = (a, b) => {
    
    const result = parseInt(a) + parseInt(b)

    return result
}

export const sub = (a, b) => {
    
    const result = parseInt(a) - parseInt(b)

    return result
}